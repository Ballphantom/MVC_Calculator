class CalculatorView {
  displayResult(num1: number, operator: string, num2: number, result: number) {
    console.log(`${num1} ${operator} ${num2} your Result: ${result}`);

  }
}

export default CalculatorView;